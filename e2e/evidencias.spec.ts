import { test, expect } from '@playwright/test'

test.describe('Evidencias', () => {
  test('Pagina empresa mostra secao setores', async ({ page }) => {
    await page.goto('/empresas')
    await expect(page).toHaveURL(/\/empresas\//, { timeout: 10000 })

    await page.getByText('Empresa Modelo Risco360 LTDA').first().click()
    await page.waitForURL(/\/empresas\//, { timeout: 10000 })

    await expect(page.getByText('Setores').first()).toBeVisible({ timeout: 5000 })
  })

  test('Configuracoes mostra secao de evidencias', async ({ page }) => {
    await page.goto('/configuracoes')
    await expect(page.getByRole('heading', { name: 'Configurações', level: 1 })).toBeVisible({ timeout: 10000 })
  })

  test('Step 6 — anexar evidencia e validar contexto do upload', async ({ page }) => {
    // 1. Navegar para levantamentos
    await page.goto('/levantamentos')
    await expect(page).toHaveURL(/\/levantamentos\//, { timeout: 10000 })

    // 2. Clicar no primeiro levantamento para abrir o wizard
    const levantamentoLink = page.getByText(/LPR_AEP/).first()
    if (!(await levantamentoLink.isVisible())) {
      test.skip('Nenhum levantamento disponivel para testar evidencia')
      return
    }
    await levantamentoLink.click()
    await page.waitForURL(/\/levantamentos\//, { timeout: 10000 })

    // 3. Navegar ate o Step 6 clicando em "Proximo" repetidamente
    for (let step = 1; step <= 6; step++) {
      const proximoBtn = page.getByRole('button', { name: /Próximo/i })
      if (await proximoBtn.isVisible({ timeout: 2000 }).catch(() => false)) {
        await proximoBtn.click()
        await page.waitForTimeout(500)
      }
    }

    // 4. Verificar que estamos no Step 6
    await expect(page.getByText(/evidência(s)? registrada(s)?/i).first()).toBeVisible({ timeout: 5000 })

    // 5. Anexar um arquivo de teste via file chooser
    const fileChooserPromise = page.waitForEvent('filechooser')
    await page.getByLabel('Capturar nova evidência fotográfica').click()
    const fileChooser = await fileChooserPromise

    const testImagePath = 'e2e/fixtures/test-evidence.jpg'
    const { mkdirSync, writeFileSync, existsSync } = await import('fs')
    const dir = 'e2e/fixtures'
    if (!existsSync(dir)) {
      mkdirSync(dir, { recursive: true })
    }
    // Create a minimal valid JPEG
    const jpegHeader = Buffer.from([
      ,
    ])
    writeFileSync(testImagePath, jpegHeader)
    await fileChooser.setFiles([testImagePath])

    // 6. Aguardar o preview aparecer (mock mode processa rapido)
    await expect(page.getByText(/Enviado/).first()).toBeVisible({ timeout: 10000 })

    // 7. Verificar que o nome do arquivo foi gerado automaticamente
    const evidencias = await page.locator('[class*="font-mono"]').all()
    expect(evidencias.length).toBeGreaterThan(0)

    // 8. Salvar o step
    const salvarBtn = page.getByRole('button', { name: /Salvar/i })
    if (await salvarBtn.isVisible({ timeout: 2000 }).catch(() => false)) {
      await salvarBtn.click()
      await page.waitForTimeout(1000)
    }

    // 9. Navegar de volta e confirmar que evidencia persiste
    await page.goto('/levantamentos')
    await page.waitForURL(/\/levantamentos\//, { timeout: 10000 })
    await levantamentoLink.click()
    await page.waitForURL(/\/levantamentos\//, { timeout: 10000 })

    // Navegar ate Step 6 novamente
    for (let step = 1; step <= 6; step++) {
      const proximoBtn = page.getByRole('button', { name: /Próximo/i })
      if (await proximoBtn.isVisible({ timeout: 2000 }).catch(() => false)) {
        await proximoBtn.click()
        await page.waitForTimeout(500)
      }
    }

    // Verificar que a evidencia ainda esta visivel
    await expect(page.getByText(/evidência(s)? registrada(s)?/i).first()).toBeVisible({ timeout: 5000 })
  })
})
